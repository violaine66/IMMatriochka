<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <img src="/images/istockphoto-622776362-612x612.jpg" alt="logo" width="65" height="65">
    <h1>IM🪆Matriochka</h1>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

   <div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav me-auto">
    <% if user_signed_in? %>
      <!-- Menu pour les utilisateurs connectés -->
      <li class="nav-item">
        <%= link_to "Mes réservations", reservations_path, class: "nav-link" %>
      </li>

      <% if @chatrooms.present? %>
        <% @chatrooms.each do |chatroom| %>
          <li class="nav-item">
            <%= link_to "Mes messages", chatroom_path(chatroom), class: "nav-link" %>
          </li>
        <% end %>
      <% end %>

      <!-- Lien pour se déconnecter -->
      <li class="nav-item">
        <%= link_to "Se déconnecter", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
      </li>
    <% else %>
      <!-- Menu pour les utilisateurs non connectés -->
      <li class="nav-item">
        <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      </li>
    <% end %>
  </ul>
</div>

  </div>
</div>
